package DIP.ProblematicCode;

public class Main {
    public static void main(String[] args) {

        /*
            DIP ::
            -> Instead of depending on concrete classes, you should depend on a abstraction.
            -> High level modules should not depend on low level modules.
         */
    }
}
